node_modules/
dist/
*.tsbuildinfo
.env
.env.*
!.env.example
*.log

# vault-managed secrets never get committed
/vault/store/

# evidence artifacts are generated at runtime; keep the folder, not raw captures
/evidence/screenshots/
/evidence/raw/

# dashboard's public copy is synced from /normalizer/data at dev/build time —
# /normalizer/data/normalized-output.json is the committed source of truth
/dashboard/public/data/

.DS_Store
Thumbs.db
